
👋 Hi, I’m D.J.A.! I'm from China, and I'm a one-person developer. My favorite directions are LLM & Game, so I'm also a game-maker. Now, TETRA is available on my repo(but I havent't trained it yet), and I also uploaded some new projects(I haven't trained them yet, too555).
Projects List: 1.TETRA: A new 1M-token(or more) context window base model with only 1B parameters network, built on Mamba-2 & Transformer & MoE. It solved the old old problem on normal Transformer models,it's the Multi-Head Self-Attention part. It has a time complexity O(n) instead of O(n)
               2.PIPIT: A frontier model witch built on a unusual architecture(I designed it 😀), used FFN, contained SFR, CFRA, LTK, MUX(hard to understand, right? ).
               3.SPARK: A fastest(but a little unstable) LLM quantize engine, witch used Block FP2(2.25bit) on some little models with the QAT.
               4.PHLOX: A LLM MAS SYS, try to beat SYMPHONY-S on Hotpot-QA(Emm...), mainly used MCTS + UCB + ECMS + 3Q-MED + SALME. Got 59% points, only used 8-9 nodes, has a high F1(70%+).
               5.PROBE: A only 27/100M parameters network model, with a new training mode worked on a new unreal net-space, witch has 90+ operators. Try to reached the goal on ARC-AGI-1/2-Public/Half Public/Private
               6.TINIA: A mRNA model with 500M parameters, try to beat Carbon-3B/Evo-7B on those mRNA sessions, becoming the new SOTA.
